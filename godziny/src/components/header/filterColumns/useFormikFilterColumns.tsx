

const useFormikFilterColumns = () => {
  const initialValues = {
    searchedName: "",
  };

  const onSubmit = () => {};

  return { initialValues, onSubmit };
};

export default useFormikFilterColumns;
